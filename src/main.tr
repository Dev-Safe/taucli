from lib import parser
from defs import Flag

def main() -> int:
    p = parser()
        .description("Tauraro cli application")
        .flag(Flag.toggle("help", "Show help"))
    print("Hello from taucli!: struct: ;")
    return 0
