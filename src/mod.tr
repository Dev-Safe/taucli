from args import Arg, ArgValue
from flags import Flag, FlagValue
from result import ParseResult
from cmds import Command

pub def parser(name: str, desc: str) -> Command:
    return Command.init(name, desc)
